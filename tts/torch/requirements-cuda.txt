# Install with:
#   pip install -r requirements-base.txt
#   pip install -r requirements-cuda.txt --index-url https://download.pytorch.org/whl/cu124
#
# torchaudio must come from this index too — qwen-tts otherwise pulls a CUDA
# PyPI wheel that may not match the torch build (e.g. libcudart.so.13).
torch
torchaudio
